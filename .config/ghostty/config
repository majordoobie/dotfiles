# Not needed just want to remember
keybind = super+shift+comma=reload_config

# Themeing
theme = Catppuccin Mocha
# theme = "catppuccin-mocha"
# 19 is the sweet spot to not get a gap in the bottom of the tmux bar
font-size = 19
font-family = "JetBrainsMono Nerd Font"
mouse-hide-while-typing = true
background-opacity = .96
#macos-titlebar-style = hidden
#macos-window-shadow = false
bold-is-bright = true
macos-option-as-alt = true


# Startup
#initial-command = /bin/zsh -l -c "tmux attach -d -t chungus || tmux new-session -s chungus"
shell-integration-features = cursor,sudo,no-title

command = /run/current-system/sw/bin/fish

# ask if the app should close on cmd + q
confirm-close-surface = false

# clipboard settings
clipboard-read = allow
copy-on-select = true
keybind = shift+enter=text:\x1b\r
